import { writable } from 'svelte/store';


export const articleList = writable([]);
export const testValue = writable(123);
export const articleListSt = writable([]);
export const buttonList = writable([]);
export const orderList = writable([]);

